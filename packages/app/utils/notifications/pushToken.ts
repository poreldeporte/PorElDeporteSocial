export const loadPushToken = async (_userId: string): Promise<string | null> => null

export const storePushToken = async (_userId: string, _token: string) => {
  //
}

export const clearPushToken = async (_userId: string) => {
  //
}
